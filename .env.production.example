# -----------------------------------------------------------------------------
# OmniCore - Production Environment (İskelet)
# Kullanım: cp .env.production.example .env.production
#          Değerleri doldurup: docker compose --env-file .env.production up -d
# -----------------------------------------------------------------------------
#
# Doldurma kontrol listesi:
#   [ ] POSTGRES_PASSWORD (güçlü şifre)
#   [ ] NEXTAUTH_SECRET   → openssl rand -base64 32
#   [ ] ENCRYPTION_KEY    → openssl rand -hex 32
#   [ ] NEXT_PUBLIC_APP_URL (örn. https://app.example.com)
#   [ ] OPENAI_API_KEY veya GEMINI_API_KEY (AI özellikleri için)
#
# İsteğe bağlı: REDIS_PASSWORD, CLOUDFLARE_*, AWS_*
# -----------------------------------------------------------------------------

NODE_ENV=production

# -----------------------------------------------------------------------------
# Veritabanı (PostgreSQL - Compose içinde postgres servisi)
# Compose ile: POSTGRES_* kullanılır; DATABASE_URL app/worker tarafından türetilir.
# Dış DB kullanıyorsanız doğrudan DATABASE_URL verin.
# -----------------------------------------------------------------------------
POSTGRES_USER=omnicore
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=omnicore

# Dış PostgreSQL kullanıyorsanız (compose'daki DATABASE_URL override edilir):
# DATABASE_URL=postgresql://USER:PASSWORD@host:5432/omnicore?schema=public

# -----------------------------------------------------------------------------
# Redis (Compose içinde redis servisi)
# Şifre opsiyonel; boş bırakılırsa Redis şifresiz çalışır.
# -----------------------------------------------------------------------------
REDIS_PASSWORD=

# Dış Redis: REDIS_URL verin (app/worker için)
# REDIS_URL=redis://:PASSWORD@host:6379

# -----------------------------------------------------------------------------
# Uygulama URL (müşteri tarafı linkleri, e-posta içeriği vb.)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=https://your-domain.com

# -----------------------------------------------------------------------------
# Auth (NextAuth.js v5) - AUTH_SECRET veya NEXTAUTH_SECRET (32+ karakter)
# openssl rand -base64 32
# -----------------------------------------------------------------------------
AUTH_SECRET=CHANGE_ME_OPENSSL_RAND_BASE64_32
# Google OAuth (opsiyonel): AUTH_GOOGLE_ID=... AUTH_GOOGLE_SECRET=...

# -----------------------------------------------------------------------------
# Pazaryeri credential şifreleme (32 byte hex = 64 karakter)
# openssl rand -hex 32
# -----------------------------------------------------------------------------
ENCRYPTION_KEY=CHANGE_ME_64_CHAR_HEX

# -----------------------------------------------------------------------------
# AI (Soru-cevap, kategori eşleştirme)
# -----------------------------------------------------------------------------
OPENAI_API_KEY=
GEMINI_API_KEY=

# -----------------------------------------------------------------------------
# Opsiyonel: Cloudflare (WAF)
# -----------------------------------------------------------------------------
CLOUDFLARE_ZONE_ID=
CLOUDFLARE_API_TOKEN=

# -----------------------------------------------------------------------------
# Opsiyonel: S3 / Yedekleme
# -----------------------------------------------------------------------------
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=eu-west-1
S3_BACKUP_BUCKET=omnicore-backups

# -----------------------------------------------------------------------------
# Rate limit (entegrasyon başına istek/dk)
# -----------------------------------------------------------------------------
RATE_LIMIT_REQUESTS_PER_MINUTE=60
